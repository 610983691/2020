#must be bootstrap.properties ,if its application.properties,somp config will not effect.for example:$ spring.cloud.nacos.config.namespace
 spring.application.name=gateway
 server.port=80
 #use with -Dspring.profiles.active=dev is a good idea.
spring.profiles.active=dev